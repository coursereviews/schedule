// coursesearch.js
// --------
define(['jquery', 'underscore', 'backbone'], function($, _, Backbone) {

  var CourseSearch = Backbone.Model.extend({

    model: singleCourse

  });

  return Model;
});
